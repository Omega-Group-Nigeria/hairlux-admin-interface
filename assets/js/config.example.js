/**
 * config.example.js — Hairlux Admin
 * Copy to config.js and fill in local values. Do not commit real secrets.
 */

// API base URL (no trailing slash)
window.API_BASE = "http://localhost:3000";

// Cloudinary cloud_name (optional; other features). KYC video uses public R2 URLs only.
// window.CLOUDINARY_CLOUD_NAME = "your_cloud_name";
window.CLOUDINARY_CLOUD_NAME = "";

// Optional: set token directly instead of reading from localStorage
// window.AUTH_TOKEN = "";
